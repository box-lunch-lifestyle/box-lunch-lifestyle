import { put, takeEvery } from 'redux-saga/effects';

function* positiveMessageSaga() {
  //yield takeEvery(ACTION, functionCall);
  console.log('placeholder in positiveMessageSaga');
};

export default positiveMessageSaga;